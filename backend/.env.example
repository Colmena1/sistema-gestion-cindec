# Copiar este archivo a .env y llenar con los valores reales (nunca subir .env a Git)
PORT=4000

DB_HOST=127.0.0.1
DB_PORT=3306
DB_NAME=cindec_db
DB_USER=root
DB_PASSWORD=

JWT_SECRET=cambia_esto_por_un_valor_secreto_largo
