# Frontend (pendiente)

Se crea con Vite + React durante la fase de Diseño/Desarrollo (semanas 41 en adelante), una vez que:

1. Los requerimientos (`docs/01-requerimientos/`) estén cerrados.
2. Los prototipos de interfaz (`docs/02-diseno/`) estén definidos.

Comando de referencia para cuando llegue el momento:

```bash
npm create vite@latest . -- --template react
```
